export type User = {
  id: string
  email: string
  name: string
  age: number
}

export type Product = {
  id: string
  title: string
  description: string
  price: string
  category: string
  quantity: string
  imageUrl: string
}
